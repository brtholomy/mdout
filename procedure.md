# 1. Run the script

run from project root:

```sh
./mdout/run.sh foo.md
```

# 2. Copy into AFP

1. Copy from docx.

2. Into one normal page master.

3. Expand to all pages.

4. Remove all styles

5. Apply Body to everything

6. Chapter title master

7. Chapter title style

8. Drop caps to first paragraph

NOTE: when mass copying, stop and iterate at this point.

# 3. Search and replace in AFP

1. Delete unused styles.

2. Run find-and-replace for all special chars and their associated style as outlined below.

## Find and apply auto_numbered_section

```
¢
\1
```

## Find and apply list long

```
 •	(.*?)
\1
```

### Apply list short

Mark in source with:

```
<!-- FORMAT: list short -->
```

NOTE: I may forgo the short versions of lists. It's a nuissance and not strictly necessary.

## Find and apply numbered list long

```
 [0-9]+?\.\t
\1
```

### Apply list numbered short

Mark in source with:

```
<!-- FORMAT: list numbered short -->
```

## Find and apply blockquote

```
¥
\1
```

### Find and apply blockquote source

```
₱
\1
```

### Apply blockquote poem

Mark in source with:

```
<!-- FORMAT: blockquote poem -->
```

OPTIONAL: right align short blockquote poem instances.

## Fix "Verbatim Char" which ODT/DOCX will insert

replace with "inline code" char style

NOTE: for blockquote sections, these are either English in Body or Chinese.

## Find and apply `<sup>`

```
¤
\1
```

NOTE: failed to show up for some reason, `<sup>` tag proceeds through rich text formatting though.

## Find and apply `<h2>`

```
£
\1
```

# 4. Manual fixes

## Fix sections and page ranges

## Fix TOC

## Fix missing fonts with character styles

### Chinese

```
[\x{4E00}-\x{9FFF}\x{3000}-\x{303F}\x{FF00}-\x{FFEF}]+
$0
```

NOTE: if i want to switch to a preprocess method:

```
perl -CSD -pe 's/([\p{Han}\x{3000}-\x{303F}\x{FF00}-\x{FFEF}]+)/[$1]{custom-style="Chinese"}/g' input.md > processed.md
```

### Japanese

NOTE: but this will match the core CJK range as Japanese. Use with caution. Or only use the first and last ranges:

```
[\x{3000}-\x{30FF}\x{4E00}-\x{9FFF}\x{FF00}-\x{FFEF}]+
$0
```

### font choices

NOTE: variable-width fonts will break afpub export, because the embed font:subset option, which embeds only those glyphs used, involves some crazy calculus when the font is variable-width. must use static-width fonts.

https://github.com/adobe-fonts/source-han-serif/tree/release#otcs

NOTE: therefore, went back to OSX system fonts, which really are fine.

* Chinese : Songti TC
* Japanese : Mincho
* Cuneiform : Noto Sans Cuneiform
* Gothic : Noto Sans Gothic

## Fix fonts manually

* Sanskrit : any with ligatures must be inserted as vectors.
* Hebrew : must be reversed manually. NOTE that git diff will display RTL languages wrong, while emacs will be correct. Export and check with a browser to be sure.

## Smart chars : left as is

* allowed single char ellipses: …
* smart quotes in place, generated by pandoc
